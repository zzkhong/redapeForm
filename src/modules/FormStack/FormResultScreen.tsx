import React from 'react';
import { SafeAreaView, Text } from 'react-native';

interface FormProps {}

const FormResultScreen: React.FC<FormProps> = ({}) => (
  <SafeAreaView>
    <Text>Form Result Screen</Text>
  </SafeAreaView>
);

export default FormResultScreen;
