import React from 'react';
import { Text } from 'react-native';

interface RadioFieldProps {}

const RadioField: React.FC<RadioFieldProps> = ({}) => (
  <Text>RadioField Input</Text>
);

export default RadioField;
